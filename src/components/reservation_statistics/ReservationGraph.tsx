import React from "react";

interface ReservationGraph {
  title: string;
  count: number;
  variance: number;
}

const ReservationGraph: React.FC<ReservationGraph> = (props) => {
  return <div></div>;
};

export default ReservationGraph;
